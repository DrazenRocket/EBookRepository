(function (angular) {
    "use strict";

    angular
        .module("app.eBook")
        .config(configRoute);

    configRoute.$inject = ["$stateProvider"];
    function configRoute($stateProvider) {

    }
} (angular));
